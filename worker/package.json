{
  "name": "magicrunes-signal",
  "version": "1.0.0",
  "private": true,
  "description": "Magic Runes — Cloudflare Worker (signaling + TURN proxy)",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "login": "wrangler login"
  },
  "devDependencies": {
    "wrangler": "^3.95.0"
  }
}
